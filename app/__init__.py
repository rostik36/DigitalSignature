"""Signature capture + mouse-replay tool.

Capture handwriting/signatures on a canvas (pen or mouse), store them to disk,
and replay them by driving the real OS mouse cursor inside a screen rectangle
you select -- e.g. to sign a digital form whose signature pad only registers a
single dot when you try to sign it by hand.
"""

__version__ = "0.1.0"
